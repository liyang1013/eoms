<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.keboda.eomsback.flux.mapper.DocAdjDetailsMapper">


    <select id="checkQty" resultType="java.math.BigDecimal">
        SELECT toQty
        FROM DOC_ADJ_DETAILS
        WHERE adjLineStatus &lt; '10' and locationid=#{locationid} AND lotNum=#{lotnum} AND traceId=#{lotnum}
    </select>
</mapper>